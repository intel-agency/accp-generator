---
name: generate-target-model-providers
description: Generate target client model provider entries (BYOK) from source model providers using the project plan.
argument-hint: targetClient=<client name>
agent: agent
---

Run the plan in [generate-target-model-providers.md](../../generate-target-model-providers.md).

Target client type: ${input:targetClient:Target client type}

Use the target client type as the argument when executing the plan.
