---
name: generate-target-mcp-servers
description: Generate target client MCP server configurations from source MCP servers using the project plan.
argument-hint: targetClient=<client name>
agent: agent
---

Run the plan in [generate-target-mcp-servers.md](../../generate-target-mcp-servers.md).

Target client type: ${input:targetClient:Target client type}

Use the target client type as the argument when executing the plan.
